/**
 * WISE System - Dashboard Page
 * Entry point for dashboard.html. Uses shared auth helpers from core/.
 */

document.addEventListener('DOMContentLoaded', function() {
  if (!initBasePage()) return;
});
