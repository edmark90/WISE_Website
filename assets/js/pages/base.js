/**
 * WISE System - Base Page Bootstrap
 * Simple shared entry point for placeholder/simple pages that only need:
 * auth guard, sidebar user info, and logout wiring (initBasePage from core/auth.js).
 */

document.addEventListener('DOMContentLoaded', function() {
  initBasePage();
});
